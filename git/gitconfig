[core]
    editor = nvim

[gpg]
    format = ssh

[commit]
    gpgsign = true

[init]
    defaultBranch = main

[push]
    autoSetupRemote = true

[include]
    # Include user details from local file
    path = ~/.dotfiles/git/gitconfig.local
